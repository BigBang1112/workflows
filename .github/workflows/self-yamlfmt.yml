name: Self — YAML Format

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write
  workflows: write

jobs:
  yamlfmt:
    uses: ./.github/workflows/yamlfmt.yml
    with:
      auto-commit: true
